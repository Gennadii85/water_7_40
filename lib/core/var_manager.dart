import 'package:flutter/material.dart';

class VarManager {
  static const cardSize = TextStyle(fontSize: 17);
  static const cardOrderStyle =
      TextStyle(fontSize: 18, color: Colors.blueAccent);
  static const String orders = 'orders';
}
